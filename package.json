{
  "name": "ti4-map-lab",
  "version": "1.2.0",
  "private": true,
  "homepage": "https://joepinion.github.io/ti4-map-lab",
  "dependencies": {
    "bulma": "^0.7.5",
    "eslint-utils": "^1.4.3",
    "gh-pages": "^2.1.1",
    "jquery": "^3.4.1",
    "local-storage": "^2.0.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-scripts": "3.1.1",
    "sass": "^1.22.10"
  },
  "scripts": {
    "start": "react-scripts --openssl-legacy-provider start",
    "build": "react-scripts --openssl-legacy-provider build",
    "predeploy": "yarn run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
